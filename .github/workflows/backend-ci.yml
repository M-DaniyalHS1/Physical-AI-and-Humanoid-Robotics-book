name: Backend CI/CD Pipeline

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.9, 3.10, 3.11]

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        cd backend
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov black mypy
    
    - name: Lint with Black
      run: |
        cd backend
        python -m black --check src/ tests/
    
    - name: Type check with MyPy
      run: |
        cd backend
        mypy src/
    
    - name: Test with pytest
      run: |
        cd backend
        python -m pytest tests/ -v --cov=src --cov-report=xml
    
    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./backend/coverage.xml
        flags: unittests
        name: codecov-umbrella

  security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Secret Scanning
      uses: trufflesecurity/truffleHog@main
      with:
        path: ./
        base: ${{ github.event.repository.default_branch }}
        head: HEAD

  build-and-deploy:
    needs: [test, security]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        cd backend
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install gunicorn
    
    - name: Run security audit
      run: |
        cd backend
        pip install bandit
        bandit -r src/ -c pyproject.toml
    
    - name: Build Docker image
      run: |
        docker build -t ai-textbook-backend:${{ github.sha }} .
        docker tag ai-textbook-backend:${{ github.sha }} ai-textbook-backend:latest
    
    - name: Login to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: Push Docker image
      run: |
        docker push ai-textbook-backend:${{ github.sha }}
        docker push ai-textbook-backend:latest
    
    - name: Deploy to production
      run: |
        # This would be where you deploy to your production environment
        # For example, using kubectl to update a deployment in Kubernetes
        # This is a placeholder for the actual deployment commands
        echo "Deploying to production environment..."
        # kubectl set image deployment/ai-textbook-backend ai-textbook-backend=ai-textbook-backend:${{ github.sha }}
        # kubectl rollout status deployment/ai-textbook-backend